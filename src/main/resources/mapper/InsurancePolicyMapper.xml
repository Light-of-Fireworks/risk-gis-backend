<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.riskgis.mapper.InsurancePolicyMapper">

    <select id="selectByBuffer" resultType="com.riskgis.model.InsurancePolicy">
        SELECT id, policy_no, policy_holder, insured_name, category_code, type_code,
               target_no, coverage_amount, premium, start_date, end_date, status, address,
               ST_AsText(location) as location, created_at
        FROM insurance_policy
        WHERE ST_DWithin(
            location::geography,
            ST_SetSRID(ST_MakePoint(#{lng}, #{lat}), 4326)::geography,
            #{radiusMeters}
        )
        <if test="categoryCodes != null and categoryCodes.size() > 0">
            AND category_code IN
            <foreach collection="categoryCodes" item="code" open="(" separator="," close=")">
                #{code}
            </foreach>
        </if>
        <if test="typeCodes != null and typeCodes.size() > 0">
            AND type_code IN
            <foreach collection="typeCodes" item="code" open="(" separator="," close=")">
                #{code}
            </foreach>
        </if>
        AND status = 'active'
    </select>

    <select id="selectByGeometry" resultType="com.riskgis.model.InsurancePolicy">
        SELECT id, policy_no, policy_holder, insured_name, category_code, type_code,
               target_no, coverage_amount, premium, start_date, end_date, status, address,
               ST_AsText(location) as location, created_at
        FROM insurance_policy
        WHERE ST_Within(location, ST_GeomFromText(#{wkt}, 4326))
        <if test="categoryCodes != null and categoryCodes.size() > 0">
            AND category_code IN
            <foreach collection="categoryCodes" item="code" open="(" separator="," close=")">
                #{code}
            </foreach>
        </if>
        <if test="typeCodes != null and typeCodes.size() > 0">
            AND type_code IN
            <foreach collection="typeCodes" item="code" open="(" separator="," close=")">
                #{code}
            </foreach>
        </if>
        AND status = 'active'
    </select>

</mapper>
